const CLOUDINARY = Object.freeze({
    TYPE: Object.freeze({
        IMAGE: "image",
        VIDEO: "video"
    }),
    FOLDER: Object.freeze({
        AVATAR: "avatars",
        LESSON: "lessons",
        THUMBNAIL: "thumbnails"
    })
});

export default CLOUDINARY;